# Add this job to your Prometheus scrape_configs
# Ensure scrape_interval is >= your exporter poll interval (default 60s)
scrape_configs:
  - job_name: 'foxess_exporter'
    scrape_interval: 60s
    static_configs:
      - targets: ['<exporter-host>:9200']
    metrics_path: /metrics
